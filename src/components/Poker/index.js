import React from "react"
// eslint-disable-next-line
import { SText, BText, Description, Container, Title, Canvas } from "./styles"

export const Welcome = () => (
    <Container>
        <Title> POKER </Title>
    </Container>
)

export const WelcomeInfo = () => (
        <SText> Instructions:</SText>
)

export const CanvaS = () => (
    <div>
        <Canvas/>
    </div>
)