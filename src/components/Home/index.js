import React from "react"
// eslint-disable-next-line
import { Container, BText, SText, Title, Button, QSign } from "./styles"

export const Welcome = () => (
  <Container>
    <QSign/> <Title>QUARANTINE FUN TIMES</Title> <QSign/>
  </Container>
)


export const WelcomeInfo = () => (
  <div>
    <SText>Make quarantine fun ~ Louie </SText>
  </div>
)